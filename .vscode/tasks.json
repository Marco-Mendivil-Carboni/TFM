{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C: build active file",
            "command": "/usr/bin/gcc",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/bin/${fileBasenameNoExtension}",
                "-lm"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build"
            }
        },
        {
            "label": "CUDA C: build active file",
            "command": "/usr/local/cuda-11.4/bin/nvcc",
            "args": [
                "-gencode",
                "arch=compute_35,code=sm_35",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/bin/${fileBasenameNoExtension}",
                "-lm",
                "-lcurand",
                "-Wno-deprecated-gpu-targets"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$nvcc"
            ],
            "group": {
                "kind": "build"
            }
        },
        {
            "label": "gnuplot: load active file",
            "command": "/usr/bin/gnuplot",
            "args": [
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gnuplot"
            ],
            "group": {
                "kind": "build"
            }
        }
    ]
}
